import { ItemSubType } from "../manifest/types";

/*export interface IStat {
  hash: number;
  name: string;
  value: number;
}

export interface IPlugItem {
  hash: number;
  name: string;
  stats: Map<number, IStat>;
}

export interface ISocket {
  type: "socket";
  socketIndex: number;
  initialPlugItem?: IPlugItem;
  plugItems: IPlugItem[];
}

export interface IMasterworkStat {
  hash: number;
  name: string;
  plugItems: IPlugItem[];
}

export interface IMasterworkSocket {
  type: "masterwork";
  socketIndex: number;
  initialPlugItem?: IPlugItem;
  stats: IMasterworkStat[];
}

export interface ISocketCategory {
  name: string;
  sockets: (ISocket | IMasterworkSocket)[];
}

export interface IInterpolationPoint {
  value: number;
  weight: number;
}

export interface IScaledStat {
  hash: number;
  displayAsNumeric: boolean;
  displayInterpolation: IInterpolationPoint[];
}

export interface IItem {
  hash: number;
  name: string;
  socketCategories: ISocketCategory[];
  stats: Map<number, IStat>;
  scaledStats: IScaledStat[];
  itemSubType: ItemSubType;
}

export interface IDisplayStat {
  hash: number;
  name: string;
  value: number;
  displayAsNumeric: boolean;
}*/
